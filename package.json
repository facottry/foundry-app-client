{
    "name": "appclient",
    "version": "3.3.9.1",
    "private": true,
    "dependencies": {
        "axios": "^0.27.0",
        "framer-motion": "^12.33.0",
        "lucide-react": "^0.563.0",
        "react": "^18.0.0",
        "react-dom": "^18.0.0",
        "react-helmet-async": "^2.0.5",
        "react-router-dom": "^6.3.0",
        "recharts": "^3.7.0",
        "sharp": "^0.34.5",
        "tailwind-scrollbar-hide": "^4.0.0"
    },
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "commit": "git add . && git commit -m 'update' && git push origin main",
        "production": "git push --force-with-lease origin main:PRODUCTION_DEPLOYMENT",
        "development": "git push --force-with-lease origin main:DEVELOPMENT_DEPLOYMENT",
        "tagversion": "node -e \"const v=require('./package.json').version; const {execSync}=require('child_process'); const d=new Date(); const ts=d.toLocaleString('en-GB',{year:'numeric',month:'short',day:'2-digit',hour:'2-digit',minute:'2-digit',hour12:true}).replace(',',''); try{execSync(`git tag -a v${v} -m \\\"Release v${v} at ${ts}\\\"`,{stdio:'inherit'})}catch(e){console.log('Tag v'+v+' already exists. Skipping.')}\"",
        "tagversionpush": "npm run tagversion && git push origin --tags",
        "FAST_DEV": "git add . && git commit -m 'FAST_DEV' && node -e \"const d=new Date(); const ts=`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}-${String(d.getHours()).padStart(2,'0')}-${String(d.getMinutes()).padStart(2,'0')}`; require('child_process').execSync(`git tag dev-${ts}`, { stdio:'inherit' })\" && git push origin HEAD:DEVELOPMENT_DEPLOYMENT && git push origin --tags"
    },
    "eslintConfig": {
        "extends": [
            "react-app",
            "react-app/jest"
        ]
    },
    "browserslist": {
        "production": [
            ">0.2%",
            "not dead",
            "not op_mini all"
        ],
        "development": [
            "last 1 chrome version",
            "last 1 firefox version",
            "last 1 safari version"
        ]
    },
    "devDependencies": {
        "@vitejs/plugin-react": "^5.1.2",
        "autoprefixer": "^10.4.16",
        "postcss": "^8.4.31",
        "tailwindcss": "^3.4.17",
        "vite": "^7.3.1"
    }
}